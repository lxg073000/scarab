import { connect } from "react-redux";
// import {} from "../../actions/user";
import UserBuggoutIndexCard from "./user_buggout_index_card";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(UserBuggoutIndexCard);
