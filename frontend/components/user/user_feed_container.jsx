import { connect } from "react-redux";
// import {} from "../../actions/user";
import UserFeedCard from "./user_profile_card";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(UserFeedCard);
