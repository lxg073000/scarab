import { connect } from "react-redux";
// import {} from "../../actions/user";
import UserRoutes from "./user_routes_card";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(UserRoutes);
