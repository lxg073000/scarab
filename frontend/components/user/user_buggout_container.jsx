import { connect } from "react-redux";
// import {} from "../../actions/user";
import UserBuggoutCard from "./user_buggout_card";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(UserBuggoutCard);
