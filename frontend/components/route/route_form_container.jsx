import { connect } from "react-redux";
import RouteForm from "./route_form";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(RouteForm);
