import { connect } from "react-redux";
// import {} from "../../actions/nav";
import NavItems from "./nav_items";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(NavItems);
