import { connect } from "react-redux";
// import {} from "../../actions/route";
import RoutePostIndexCard from "./post_index_container";

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(RoutePostIndexCard);
